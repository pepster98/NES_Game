.SEGMENT "CHARS"
	;.INCBIN "CHR.BIN"
	
.SEGMENT "HEADER"
	.BYTE "NES"
	.BYTE 26
	.BYTE 2
	.BYTE 1
	
.SEGMENT "VECTORS"
	.WORD 0, 0, 0, NMI, RESET, IRQ
	
.SEGMENT "STARTUP"
.SEGMENT "CODE"

NMI:
IRQ:
	RTI
RESET:
	SEI
	CLD
	LDX #$40
	STX $4017
	LDX #$FF
	TXS
	INX
	STX $2000
	STX $2001
	STX $4010
	
vblankwait1:
	BIT $2002
	BPL vblankwait1
	

FOREVER:
	JMP FOREVER